# MashiroBot 插件开发经验与教训

这份文档是新插件的必读清单。它合并了计划插件、游戏计时插件和 Windows 定时任务维护中已经验证过的经验；新插件应优先复用现有 `core`、SQLite、任务注册和 worker 模式，不要重新发明一套。

## 必须遵守

1. **插件边界**：业务代码放在 `plan/plugin/mashirobot-plugin-*`；全局路由、帮助菜单、日志和桥接放在 `plan/core`。每个插件必须有 `plugin.json`、`index.mjs`、独立 `README.md` 和独立测试。
2. **本地优先**：能由精确文本、数据库或脚本完成的指令，在 GPT 前处理；插件执行失败只返回本地错误，不要把同一消息再次交给 GPT。
3. **状态集中**：正式数据放 `plan/sqlite/openclaw-planner.sqlite`，不要在插件目录散落长期 JSON、cache、state 或 logs。临时文件使用 `%TEMP%`，日志使用 `%USERPROFILE%\.openclaw\logs\mashirobot.log`。
4. **持久 worker**：Windows 任务不能引用会被迁移、删除或改名的工作区脚本。安装时复制版本化 worker 到 `%LOCALAPPDATA%\MashiroBot\<plugin>\workers`，任务动作使用 PowerShell 7 的完整路径。
5. **路径安全**：关闭进程、修改文件或删除任务前，先确认目标的完整路径和明确边界。游戏进程必须同时匹配进程名和完整可执行路径，不得按名称误杀 Steam 或其他程序。
6. **睡眠与唤醒**：`WakeToRun` 只能唤醒睡眠/休眠，不能让关机电脑执行任务；任务依赖网络或微信时，电脑必须已开机并保持网络可用。

## 时间与任务计划

- **日期处理统一参考计划插件**：以 `plan/plugin/mashirobot-plugin-plan/windows/manage-plan.ps1` 和 `routine-reminder.ps1` 为唯一范式，不在新插件中重新设计日期转换。
- 注册 Windows 任务时解析 `DateTimeOffset`，使用 `.LocalDateTime` 创建 `New-ScheduledTaskTrigger`，创建完整 `New-ScheduledTask` 定义，注册后显式 `Enable-ScheduledTask`。
- 任务注册后的实际时间必须通过 `Export-ScheduledTask` 读取落盘 XML 校验，不能只相信刚注册对象的缓存值。
- 不要只读取刚注册对象的缓存值校验时间；用 `Export-ScheduledTask` 读取落盘 XML，确认实际 `StartBoundary`。
- 一次性任务必须有自清理逻辑；失败、取消、覆盖和过期状态都要释放数据库锁。任务名只接受本插件前缀和数据库中保存的名称。
- 任务要设置 `StartWhenAvailable`、`WakeToRun`、电池运行选项和合理的执行时限；不要用过短的未来时间作为唯一测试依据。

## Windows 与编码

- PowerShell 脚本开头统一设置无 BOM UTF-8 输出：`$OutputEncoding` 和 `[Console]::OutputEncoding`。
- 不要让中文 JSON 经过 CMD 参数传递。Node 与 PowerShell 之间使用 Base64 UTF-8；微信发送直接调用 `openclaw.ps1`，不要依赖 `openclaw.cmd` 的控制台编码。
- 任务动作不要写相对的 `pwsh.exe`。用 `(Get-Command pwsh.exe).Source` 或当前 PowerShell 进程的完整路径，并验证文件确实存在。
- 错误信息要写入统一日志，同时保留数据库中的失败状态；微信发送失败应有限次数重试，不能无限循环。

## 进程关闭与桌面显示

- 游戏限时应采用“正常关闭请求 → 最多等待 3 秒 → 精确匹配进程强退”的两阶段流程。正常关闭使用 `CloseMainWindow()`，无窗口或不响应才进入强退。
- 独占全屏游戏退出时可能切换分辨率、刷新率或 HDR，出现几秒黑屏；这不一定是强退代码或显卡崩溃。应记录关闭前后的显示模式和系统显卡事件再判断。
- 若全屏配置为 `160Hz` 而桌面为 `60Hz`，退出黑屏通常是显示模式切换；优先让游戏使用无边框/窗口模式，或使桌面和游戏刷新率一致。

## 测试与交付

每个插件至少完成以下验证：

1. Node 核心测试：解析、路由、数据库、锁定、覆盖、过期状态和帮助指令。
2. PowerShell worker 测试：正常关闭、强制回退、路径不匹配保护、数据库状态和任务自清理。
3. 至少一次真实 Windows 任务计划隔离测试：使用临时数据库和临时进程，不使用真实游戏；确认任务实际运行、返回码为 `0x0`、状态更新并自动删除。
4. 对日期边界、系统本地时间、睡眠唤醒和网络失败分别做边界检查。
5. 测试完成后删除临时任务、临时进程和临时数据库；确认没有遗留正式任务或锁。

## 维护原则

- 新增或修改插件后同步更新插件自己的 README；根 README 只维护架构、公共规则和插件索引。
- 不为一次故障保留多套废弃实现；确认新实现通过隔离测试后删除旧代码，并保留迁移说明。
- 遇到重复故障先查这份文档和现有计划插件，再修改代码；优先修复可复用的公共边界，而不是增加临时特判。
