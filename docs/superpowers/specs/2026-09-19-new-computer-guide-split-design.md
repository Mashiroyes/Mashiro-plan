# 新电脑部署指南拆分设计

## 目标

降低根 `README.md` 的信息密度，同时保留完整、可执行的新电脑部署说明。

## 文件边界

- `docs/new-computer-setup.md`：保存环境版本、依赖、OpenClaw/微信配置、Python 路径、数据库迁移、本机状态、温度监控不迁移、安装顺序和验收命令。
- `README.md`：只保留一个简短的“新电脑部署”入口，并明确数据库不随 GitHub 恢复、温度监控不迁移、完整步骤见独立文档。

## 不变项

- 不修改脚本、依赖版本、安装命令或部署行为。
- 不恢复温度监控迁移。
- 不提交数据库、凭据或本机运行时状态。

## 验证

- 根 README 不再包含完整换机步骤。
- 独立文档包含原有全部换机内容。
- README 中的相对链接可解析，文档引用的仓库内安装脚本均存在。
